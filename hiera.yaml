---
version: 5
defaults:
  data_dir: hiera
  data_hash: yaml_data

hierarchy:
  - name: "Environmental overrides"
    path: "environments/%{environment}.yaml"
    data_dir: hiera
  - name: "Fall through defaults"
    path: "defaults.yaml"
    data_dir: hiera
